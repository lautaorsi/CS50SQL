CREATE VIEW "available" AS SELECT listings.id, listings.property_type, listings.host_name, availabilities.date FROM listings INNER JOIN availabilities ON availabilities.available LIKE "TRUE" AND listings.id = availabilities.listing_id;
